import React from "react";

const ToolBar = () => {
  return (
    <nav className="toolbar">
      <h1>Lambda Notes</h1>

      <button>PlaceHolder</button>
      <button>PlaceHolder</button>
    </nav>
  );
};

export default ToolBar;
